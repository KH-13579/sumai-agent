## 8. 実装ファイル（index.html）の具体的な不具合・修正要件
現在の実装ファイル（`frontend/index.html`）には、マージミスやコピー＆ペーストの失敗に起因する重大な構文エラー・表示バグが複数残存しています。以下の項目を至急修正してください。
- **HTMLのタグ閉じ忘れとリストの重複**: フロー表示領域の番号のズレ（「③ 法規チェック」「③ メーカー推薦」の重複など）や `</div>` の閉じ忘れ、および「Phase 2 予定」リストの記述重複を解消する。
- **CTAバナーのテキスト重複**: 送客バナー内の説明文が二重に記載されているため、正しい1文に整理する。
- **関数定義の構文エラー**: `renderLegal` 関数が閉じられないまま別の関数（`updateFlowStep`）が混入し、JavaScriptの実行を妨げている箇所を修正する。
- **フロー制御ロジックの不整合**: フロー制御用の配列 `FLOW_STEPS` に `step-maker` が抜けており、フロー進行のハイライトがずれる問題を修正する。
- **変数未定義エラーと処理の重複**: `showCtaBanner` 関数内で変数が定義される前に使用されて発生する `ReferenceError` や、同一要素へのDOM更新が連続して行われている冗長な処理を修正する。
- **チャット送信完了後の分岐エラー**: `handleSend` 関数の末尾にある `if (data.done)` ブロックが二重に記述され、矛盾した条件分岐となっている箇所を統合・修正する。

## 9. デモ版サイト（`frontend/index.html`）のUI評価

8章が「動かない箇所」の指摘であるのに対し、本章は**構文エラーが修正され正常に動作する状態を前提とした UI 品質**の評価です。現状の実装は単一ファイル・バニラJS構成で、デモ用途としては合理的ですが、以下の点に改善余地があります。

### 9.1 アクセシビリティ（ARIA属性の使用が現状 0 件）
- **チャット領域がライブリージョンでない**: `#chat-messages` に `role="log"` / `aria-live="polite"` が無く、スクリーンリーダー利用者にAIの応答が読み上げられない。チャットUIとしては最も基本的な欠落。
- **入力欄にラベルが無い**: `#user-input`（608-612行）は `placeholder` のみで `<label>` も `aria-label` も無い。placeholder は入力を始めると消えるためラベルの代替にならない。
- **処理中であることが視覚以外に伝わらない**: タイピングインジケーター（760-773行）は3点の点滅のみ。`role="status"` と視覚的に隠したテキスト（例:「回答を生成しています」）を持たせ、送信ボタンにも `aria-busy` またはラベル変更（「送信中…」）を行う。
- **コントラスト不足（WCAG AA 未達）**: `#94a3b8` が23箇所で使われているが、白背景に対し約 2.6:1 で基準（4.5:1）を大きく下回る。特に**最も読みにくい色が免責文（`.disclaimer` 311-317行）に使われている**のは、責任範囲の告知として問題がある。`.req-missing`、`.plan-note`、`.col-basis`、`.flag-basis`、フローの未着手ステップ、Phase 2カードも同様。`#64748b`（約 4.8:1）以上へ引き上げる。
- **重要度が色（絵文字）のみで表現されている**: `SEVERITY_MARK` の 🔴🟡⚪（887行）には色以外の手掛かりが無い。「高／中／低」のテキスト併記と `aria-label` を付与する。判定ステータス（`LEGAL_STATUS_LABEL`）はテキストを併記しており良い実装なので、そちらに揃える。
- **フォーカス可視化が一箇所のみ**: `:focus-visible` の定義は `.maker-chip`（424行）だけ。加えて `#user-input` は `outline: none`（288行）で既定の輪郭を消している。全インタラクティブ要素に統一したフォーカスリングを定義する。
- **間取り図タブがタブとして実装されていない**: `.floorplan-tab`（853-855行）に `role="tablist"` / `role="tab"` / `aria-selected` が無く、キーボードでの左右移動もできない。さらにインライン `onclick` 属性と、`btn.textContent === '${floor}F'` という表示文字列での照合（869行）は壊れやすいので `data-floor` 属性＋`addEventListener` に置き換える。
- **`prefers-reduced-motion` 未対応**: `fadeIn`（163行）と `bounce`（271行）のアニメーションを止める手段が無い。ガイドHTML側には実装済み（285-287行）で、デモ側だけが抜けている。
- **スキップリンク／ランドマークラベルが無い**: サイドバーを読み飛ばしてチャットへ移動する導線と、`<aside>` への `aria-label` を追加する。

### 9.2 レスポンシブ（メディアクエリが 1 つしか無い）
- **ブレークポイントが実質1つ**: `@media (max-width: 900px) { .right-panel { display: none; } }`（456行）のみ。`main`（42-50行）は横並び固定、`.sidebar`（53-59行）は `width: 260px` 固定で縦積みに切り替わらないため、幅 600px 程度ではチャット領域が約 300px まで潰れる。`max-width: 760px` で `flex-direction: column` に切り替え、サイドバーは全幅かつ `<details>` で折りたたむ構成にする。
- **ヘッダーが横溢れする**: `header`（19-31行）は `height: 60px` 固定・`flex-wrap` 無し。スマートフォン幅ではタイトルとバッジが収まらず、ページ全体に横スクロールが発生する。
- **右パネルを消すと「機能」が失われる**: 900px 以下で隠れるのは装飾ではなく、フロー表示と**成果物（アーティファクト）へのリンク**（643-650行）。スマートフォン利用者は成果物に到達できなくなる。狭幅時はチャット下部か送客バナー内に再配置する。
- **法規チェック表に横スクロール用ラッパーが無い**: 5列構成の `.legal-items`（993-996行）は狭幅で確実に破綻する。ガイドHTML側の `.tablewrap`（206行）と同等の `overflow-x: auto` を用意し、狭幅では「項目名＋値」の縦積みカード表示への切り替えも検討する。
- **iOS で入力欄フォーカス時に意図しないズームが起きる**: `#user-input` の `font-size: 0.9rem`（約14.4px、285行）。iOS Safari は 16px 未満の入力欄でフォーカス時に自動ズームするため、16px 以上を指定する。
- **縦方向の適応が無い**: `#chat-messages` の `min-height: 460px` / `max-height: 600px`（154-155行）は固定値。画面高の低いノートPCでは入力欄と免責文が画面外に出て、大画面では逆に余白が余る。`clamp()` や `dvh` を用いた可変指定にする。
- **タップ領域が小さい**: `.floorplan-tab`（234-242行）は `padding: 3px 10px` で高さ約20px。推奨される最小タップ領域（44×44px）を下回る。

### 9.3 日本語入力とチャット操作（デモ進行に直結）
- **【最優先】IME変換確定の Enter で誤送信される**: キーダウンのハンドラ（1192-1197行）に `e.isComposing` の判定が無い。日本語入力では**変換候補を確定する Enter がそのまま送信になる**ため、デモ中に高い確率で意図しない文章が送信される。`if (e.key === 'Enter' && !e.shiftKey && !e.isComposing)` とする。ガイド05章のとおり応答キャッシュは発話文言の完全一致が命中条件なので、**誤送信＝キャッシュ外れ＝実機推論で数十分待ち**に直結する。UI上の不具合としては最も影響が大きい。
- **送信中もテキストエリアが編集可能**: `isLoading` によるガードはあるが、入力しても何も起きないため壊れているように見える。`disabled` 化かプレースホルダー変更で状態を明示する。
- **デモシナリオの選択が入力欄を無警告で上書きする**（724行）。入力途中の文章が消えるため、確認するか末尾に追記する挙動にする。
- **「新しいセッション」が確認なしで全消去する**（1200-1221行）。取り消し手段の無い破壊的操作なので確認を挟む。
- **リロードで会話が消える**: `sessionId` はページ読み込みのたびに再生成される（668行）。`sessionStorage` での復元、または「リロードすると会話が失われます」の明示が必要。
- **エラー表示がAIの発話と同じ吹き出し**（1175行）。システム由来のエラーであることが判別できず、再試行の導線も無い。エラー専用のスタイルと「再試行」ボタンを設ける。プリセット取得失敗（697-698行）も「取得失敗」の文言のみで復帰手段が無い。

### 9.4 情報設計・画面フロー
- **成果物が会話の外に積み上がる**: `#plans-container` / `#legal-container` / `#maker-rec-container`（591-596行）はチャットボックスの外側、入力欄の上に配置されている。そのため生成物が会話の時系列から切り離され、出力のたびに入力欄が下へ押し出される。生成物はメッセージとしてチャット内に差し込むか、右カラムの成果物タブに集約するのが自然。
- **生成完了に気づけない**: 結果は `hidden` の解除で突然出現するだけで、チャットは自動スクロールするのに結果領域はしない。描画後の `scrollIntoView` と「間取り3案ができました」等の一文を追加する。
- **成果物リンクが2系統ある**: `renderPlans` は `/artifact/{id}/download`「アーティファクト(JSON)をダウンロード」（821行）、`showArtifactLink` は `/artifact/{id}`「アーティファクトを開く」（1060行）。同じ成果物が別ラベル・別URLで2箇所に出ており紛らわしい。一本化する。
- **フロー表示と実装内容が一致していない**: フローの番号が ①②③③④（法規チェックとメーカー推薦が両方③、626-639行）、Phase 2カードは ⑤⑥⑦ と ④⑤⑥ が重複列挙されている（654-660行）。さらに**メーカー推薦AIは実装済みなのに「Phase 2 予定」に残っている**。8章の構文修正と併せ、①ヒアリング／②間取り生成／③法規チェック／④メーカー推薦／⑤送客に振り直す。
- **送客チップの情報がツールチップ頼み**: 得意分野と価格帯を `title` 属性のみで提供している（1108-1112行）ため、タッチ端末とキーボード操作では読めない。同じ情報は推薦カード（1064-1100行）に既に表示されているので、バナー側は行動導線（リンク）に絞る。
- **印刷／PDF出力の手段が無い**: 送客バナー自身が「ご提案した間取り案を持参すれば商談がスムーズになります」と案内している（601-602行）にもかかわらず、持ち出せる形式は機械可読JSONのみ。`@media print` で要件書・間取り3案・法規チェック結果を1枚にまとめるだけで送客の実効性が上がる。UI面では最も費用対効果の高い追加。
- **押せないカードにホバー効果がある**: `.plan-card:hover` の枠線・影の変化（206行）はクリック可能に見えるが実際には非対話。拡大表示等の機能を付けるか、ホバー効果を外す。

### 9.5 視覚デザイン・保守性
- **CSS変数が無い**: `#2563a8` `#1a4a7a` `#94a3b8` 等の色リテラルが全編に直書きされている。ガイドHTMLは `:root` でトークン設計されている（5-37行）ので、デモ側もトークン化し、2ファイルで同じ定義を共有する。
- **ダークモード非対応**: ガイドHTMLは完全対応、デモは白背景固定。OSのダークテーマ環境で並べると別プロダクトに見える。
- **フロー表示のスタイルがJS内の文字列**: `style.cssText` を6つの定数から組み立てている（926-932行、1032-1035行）。8章で指摘済みの重複により2セット存在してしまってもいる。`.step.is-active` / `.is-done` / `.is-idle` のクラス切り替えに置き換えれば、重複の温床そのものが無くなる。
- **文字が全体に小さい**: 本文相当が 0.72〜0.88rem（約11.5〜14px）中心。住宅検討層は年齢層の幅が広いため、本文 0.95rem 以上、補助情報でも 0.8rem を下限とする。
- **フォント指定が2ファイルで不一致**: デモは `-apple-system, "Hiragino Kaku Gothic ProN", "Meiryo"`（11行目）、ガイドは Noto Sans JP / Noto Serif JP。Windows では Meiryo が選ばれ、行間の詰まった印象になる。共通のフォントスタックを定義する。
- **`<head>` の情報が不足**: `<meta name="description">`、favicon、OGP が無く、社内でURLを共有した際の見え方が悪い。
- **バッジ配置が場当たり的**: `.maker-rank-badge` の `top: -8px` 絶対配置（335-345行）を、`.maker-type-label` の `margin: 14px 0 4px`（348-353行）で回避している。カード上部に領域を確保する構造に改める。
- **間取り図の画像に寸法指定が無い**（857行）。読み込み完了時にレイアウトシフト（CLS）が発生する。`width`/`height` または `aspect-ratio` を指定する。

### 9.6 セキュリティ（UI描画層）
- **`renderPresets` のみエスケープが抜けている**: `p.name` / `p.description` を `escHtml` を通さず `innerHTML` に埋め込んでいる（713-718行）。他の描画関数はすべてエスケープしているため、ここだけが例外になっている。
- **リンクURLの検証レベルが不統一**: `renderMakerRecommendations` は `rec.website` を `escHtml` のみで `href` に入れる（1087行）が、`showCtaBanner` は `^https?://` を検証している（1109行）。同じ用途で防御の強さが異なるので、URL検証を共通関数に切り出して両方から使う。

---

## 10. ドキュメント（`docs/法規チェックAI_ガイド.html`）のUI評価

まず良い点として、**トークンによる色設計、ダークモード完全対応、表の横スクロールラッパー、`prefers-reduced-motion`、`:focus-visible`、`text-wrap: balance`、chipによる状態表現**が揃っており、UI品質はデモ側より明確に高い。これらは**デモ側へ移植する価値がある実装**です。

### 10.1 単体HTMLとして致命的な欠落
- **`<!DOCTYPE html>` / `<html lang="ja">` / `<head>` / `<meta charset="UTF-8">` / `<meta name="viewport">` がすべて存在しない**（ファイル先頭がいきなり `<title>`）。このファイルはアプリから配信されておらず（`app/main.py` は `frontend/` のみを mount）、ローカルで直接開く前提のため、以下が実際に発生する。
  - doctype が無いため**互換モード（quirks mode）**で描画され、レイアウトが指定どおりにならない。
  - charset 宣言が無いため、環境によっては**日本語が文字化けする**。
  - viewport 指定が無いため、スマートフォン・タブレットでは 980px 幅として描画され全体が縮小表示される。
  - `lang="ja"` が無いため、スクリーンリーダーの読み上げ言語判定と禁則処理が正しく働かない。

  配布用の単体HTMLとして、最低限これらを補うこと（生成ツールから書き出した際に欠落しやすい箇所）。

### 10.2 その他の改善点
- **WebフォントがGoogle Fonts依存**（2行目）。本ガイド自体が「会場のネットワーク断でもデモを完走できること」（NFR-06）を繰り返し強調しているにもかかわらず、**ガイドを開く環境がオフラインだと想定の書体で表示されない**。フォントをローカル同梱するか、フォールバック前提のデザインにする。
- **構成図がASCIIアート**（`figure.diagram` 638-648行、695-707行）。スクリーンリーダーでは意味が伝わらず、文字幅に依存するため環境によって崩れる。`figcaption` に処理の流れを文章で補足するか、SVG／Mermaid 化する。
- **長文なのに目次が追従しない**: 全10章あるが `nav.toc`（305-319行）はページ上部に一度だけ。スクロール追従するサイドバー目次、または各章末に「目次へ戻る」を置く。
- **見出しアンカーが章（section）単位のみ**: h3 にも id を振ると「10章の②坪／㎡問題」のようにピンポイントで共有できる。
- **印刷スタイルが未定義**: 手順書は印刷して手元に置かれることが多い。特に `figure.term` の暗色背景は印刷でインクを浪費し、かつ判読しづらい。`@media print` で反転させる。
- **`<meta name="description">` が無い**（10.1 と合わせて対応）。

---

## 11. 対応の優先順位

| 優先度 | 項目 | 該当 | 理由 |
| --- | --- | --- | --- |
| **最優先** | 8章の構文エラー全件 | 8 | 現状そのままではJSが正しく動作しない |
| **最優先** | IME変換確定Enterでの誤送信 | 9.3 | デモ中に高確率で発生し、キャッシュ外れ＝数十分待ちに直結 |
| **最優先** | ガイドHTMLのdoctype・charset・viewport欠落 | 10.1 | 文字化け・互換モード描画のリスク |
| 高 | コントラスト不足（特に免責文） | 9.1 | 責任範囲の告知が最も読みにくい状態 |
| 高 | スマホで成果物リンクに到達不能 | 9.2 | 装飾ではなく機能が失われている |
| 高 | プリセットの未エスケープ／href検証の不統一 | 9.6 | 他箇所は対策済みで、ここだけが穴 |
| 高 | フロー番号とPhase 2表記の実装との不一致 | 9.4 | デモ説明時に矛盾が露呈する |
| 中 | ライブリージョン等のARIA付与 | 9.1 | チャットUIとして基本的な欠落 |
| 中 | 縦積みブレークポイントと表の横スクロール | 9.2 | 狭幅で表示が破綻する |
| 中 | 成果物の配置と重複リンクの整理 | 9.4 | 会話の文脈から成果物が切り離されている |
| 中 | 印刷／PDF出力 | 9.4, 10.2 | 「持参して商談」という送客導線を実際に成立させる |
| 低 | CSSトークン共有とダークモード対応 | 9.5 | 2ファイル間の一貫性 |
| 低 | フォントスタックと文字サイズの見直し | 9.5 | 幅広い年齢層への配慮 |
| 低 | 目次の追従・構成図のSVG化 | 10.2 | 長文ドキュメントの回遊性 |